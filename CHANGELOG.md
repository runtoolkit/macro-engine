# Changelog

## 1.0.4
- Added safe script parsing for single and multi-command text input
- Added middleware pipeline support
- Fixed multi-command concurrency handling
- Added timeout support for command execution
- Exposed new `script` and `middleware` modules

## 1.0.3
- Internal stability and API cleanup
